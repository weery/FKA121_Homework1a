% Plot the average temperature
close % close previous figure

data = importdata('histogram.dat');


% plot
figure;
%%plot(data(:,2))

g=data(:,3)./data(:,4);

plot(data(:,1),g)

nDats=50;

integr=@(q)sum(data(1:nDats,1).^2.*(g(1:nDats)-1)*sin(q*data(1:nDats,1))/(data(1:nDats,1)*q))/nDats;

s=@(q)1+4*pi*100*integr(q);

a=linspace(0,10,100);

xlim([0,data(end,1)])