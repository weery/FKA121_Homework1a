%clear all, close all, clc
clf
addpath( genpath('.') );

for i = 1:4
    s = strcat('energy0.0',int2str(i),'.dat');
    data = importdata(s);
    hold on
    plot(data(:,1),data(:,3));
end

text(5,10000000,'\downarrow 0.01')
text(4,10000000,'\downarrow 0.02')
text(7,25000000,'0.03 \downarrow')
text(6,73000000,'0.04 \downarrow')

% labels
xlabel('Time / [ps]','Interpreter','Latex');
ylabel('Energy / [eV]','Interpreter','Latex');


title('Awesome title','Interpreter','Latex')
